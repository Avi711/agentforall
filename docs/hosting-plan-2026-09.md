# Hosting and reliability plan (2026-09-13)

Decision: stay on GCE VMs + Docker, one orchestrator, N worker VMs. No GKE.
Grounds: single-copy stateful pods recover in ~15 min on GKE (10 min NotReady before node repair, pod stuck until node recreated) vs ~3 min GCE auto-restart; GKE node upgrades drain nodes on Google's schedule, GCE live-migrates; ~25% more per bot. Independent review 2026-09-13 reached the same verdict.
Sources: docs.cloud.google.com/kubernetes-engine/docs/how-to/node-auto-repair, kubernetes.io/docs/tasks/run-application/force-delete-stateful-set-pod, docs.cloud.google.com/compute/docs/instances/host-maintenance-overview, ona.com/stories/we-are-leaving-kubernetes.

## Verified state (prod, 2026-09-13)

- VM `agent-forall` e2-highmem-4: `automaticRestart=true`, `onHostMaintenance=MIGRATE`, up 134 days. 17 bots, 12/32 GB used.
- Snapshots: `agent-forall-daily-snapshot` 03:00, 14 days, attached to boot and `agent-forall-data` (80 GB). Never restore-tested.
- Alerts: disk 75%/85% policies exist with **0 notification channels**. No uptime check, no memory alert.
- Health monitor: GET `/healthz` every 15 s (1–5 ms on all bots). Marks degraded/unhealthy, never restarts.
- `docker-socket-proxy` is on `tenant-net` with `POST=1`, no auth. A tenant container reached `/containers/json` (verified). **Critical.**
- Startup-script drift: deployed metadata lacks two secret fetches; reboot unsafe (handoff issue 3).
- Orchestrator is host-scoped: `ORCHESTRATOR_HOST_ID=agent-forall-vm`, repository filters every read by `host_id`, one Docker client from `DOCKER_HOST`.
- Bots are never reached from the internet. Caddy fronts only `api.agentforall.co.il` → orchestrator. WhatsApp (Baileys), Telegram (long-poll) and the Cloud API relay are all outbound from the bot.

## 0. Isolate the Docker proxy — DONE 2026-09-13

`infra/startup.sh` compose: `control-net` (`internal: true`), proxy on it only, orchestrator on `frontend` + `tenant-net` + `control-net`. Dev `docker-compose.yml` already had the proxy on its own network. Prod: compose file replaced (backup `docker-compose.yml.bak-20260913-controlnet`), `docker compose up -d` recreated proxy + orchestrator (~15 s API blip, bots untouched). Verified: tenant curl to proxy fails, API 200, reconciliation clean, no health changes. Recorded as S-0 in `docs/security-todo.md`.

## 1. Reliability on the current VM (≈1 week)

1. **Auto-restart hung bots — DONE 2026-09-13** (orchestrator `509f9106`). `services/auto-restarter.ts` observes one liveness report per health poll: 4 consecutive "down" samples (settled, running container whose `/healthz` failed; booting, missing or Docker-unreachable containers are never "down") → `InstanceManager.restartBySystem`, which re-checks inside the lock (running, not booting, current image, still no answer), skips creds re-injection and never parks the bot in `error`. 10 min cooldown, 3 per hour, then one `instance.auto_restart_exhausted` event + error log (alert on it in item 3). Suspended while >50% of the fleet fails at once. Events in the existing `instance_events` table. 3 independent reviews; 422 tests.
2. **Reboot-safe VM — DONE 2026-09-13 (reboot exercised 22:45 UTC: bots back at +95 s, script clean, all checks passed).** Script moved to `metadata["startup-script"]` (in place, no ForceNew), state re-imported, data disk + alerts + IAM now in Terraform, plan empty. Script now owns the data-disk mount and Docker data-root (`RequiresMountsFor`), fails loudly instead of starting on the boot disk. Verified byte-identical from the VM's metadata server; data-disk block exercised on the live VM. A clone test was dropped: a clone would boot against the prod database and WhatsApp sessions. Follow-up: the script pre-pulls the Hermes image before `compose up`, which delayed the "platform started" marker by ~3 min (services were already up via restart policies).
3. **Alerts — DONE 2026-09-13** (`infra/monitoring.tf`). Email channel (`alert_email` in tfvars), uptime check on `/health` every 60 s + "API unreachable" policy, VM memory > 85 % for 5 min, the two disk policies wired, and a log-based policy on the orchestrator's "auto restart budget exhausted" / "most bots failed liveness at once" lines. Orchestrator logs ship via Docker's `gcplogs` driver (`docker logs` still works locally); info-level lines are excluded from ingestion. Telegram delivery later needs a webhook relay.
4. **Restore test — DONE 2026-09-13.** Disk from the day's snapshot attached read-only to a credential-less scratch VM: 17 tenant volumes present, `openclaw-agent.sqlite` and `state/openclaw.sqlite` open with `integrity_check` ok (copy the `-wal`/`-shm` files alongside before opening). Scratch VM and disk deleted afterwards.
5. **Memory — cap + watch DONE 2026-09-14** (orchestrator `559836e4`). `DEFAULT_RESOURCE_LIMITS.memoryMb` 4096 → 3072 for new bots (existing rows keep their stored limit; API max stays 4096). `services/memory-watch.ts` samples every bot's usage every 5 min (`docker stats` formula) and logs `bot memory high` once at 80 % of its limit; the "orchestrator needs attention" alert matches it. **Chromium tab cleanup: deferred** — closing tabs a bot may be using is a customer-visible risk; needs an idle signal (no active session, no cron due) before any CDP-driven close. Design separately (handoff issue 4).

6. **Health monitor at scale — DONE 2026-09-14** (orchestrator `d2484033`). A healthy `running` row is written only when `last_seen_at` is older than 60 s (or on any status/failure change); Docker is consulted once a minute per healthy bot (repairing a lagging container id) and otherwise only to classify a failed probe. The reconciler resolves containers by name before marking a row `error`. Poll interval stays 15 s. At 1,000 bots: ~17 writes/s → ~1/s-equivalent per minute tick instead of ~70/s.

Final audit 2026-09-14 (orchestrator `1c3509d8`): blocked restarts (other image, no container) spend budget and log `instance.auto_restart_blocked` so the exhausted alert still fires; cooldown no longer depends on the budget window; reconciler skips rows under an operation lock; `startup.sh` refuses to format the data disk once bootstrapped. Two independent reviews, 450 tests.

## 2. Multi-host (≈3 weeks, before a campaign that can bring hundreds)

Design: one orchestrator VM (e2-small, reserved internal IP) + identical worker VMs in a dedicated VPC (no `default-allow-internal`), no public IPs, egress via Cloud NAT. Workers run `docker-socket-proxy` + bots. Orchestrator → worker: Docker's own remote protocol over mTLS on `:2376` (dockerode, zero new RPC code) plus the bots' gateway ports bound to the worker's private IP; one firewall rule, tag `orchestrator` → tag `worker`, ports 2376 + 19000-19999. Bots → orchestrator: `https://orchestrator.internal` (private Cloud DNS; Caddy internal CA with root + key seeded from Secret Manager; root mounted read-only into bots and sidecars with `NODE_EXTRA_CA_CERTS`). Control-plane certs come from a separate CA (orchestrator client cert `clientAuth`, per-worker server cert with IP SAN), never Caddy's. Two service accounts: `worker` (GAR pull, logs, metrics, its own TLS secret) and `orchestrator` (the rest); `startup.sh` takes a role.

Reviewed 2026-09-14 by three independent reviewers (design, security, SRE). Rejected with reasons: dial-out worker agent (the orchestrator needs inbound TCP to bots anyway; custom RPC with head-of-line blocking), Nomad (cannot copy files into a task, Raft quorum to run, disconnect semantics fight single-copy stateful jobs), Google CAS (no gain over Caddy's CA for an internal endpoint), automatic worker reset (split-brain: the orchestrator may be the partitioned side), GKE (single-copy recovery still ~15 min in 2026). Found live and fixed the same day: S-13, bots could reach the metadata server.

Non-negotiable before any bot is flipped to the new address:
- Relay URLs derived from `ORCHESTRATOR_INTERNAL_URL` at render time; the per-bot stored copies removed by a one-shot migration (0014). DONE 2026-09-14, slice 2.
- Caddy internal site is an allowlist of the three relay paths; the public site also 404s `/internal/*` (DONE 2026-09-14; `/api/v1/admin/*` stays public: the dashboard's admin panel calls it through the public API with the service token); relay rate limits keyed on instance id (not socket address); `TRUST_PROXY` = frontend CIDR; the orchestrator leaves `tenant-net` once Caddy is the only path.
- Per-host circuit breaker: a host whose Docker is unreachable is skipped by the health monitor and reconciler (no failure increments, one log line per transition); reconciler catches per row; startup reconcile logs instead of exiting; `/health` = process + DB only.
- Worker reset stays manual (alert + one command) until a quorum rule (never when >50 % of workers are missing) and an orchestrator-independent signal (Ops Agent uptime) exist. Workers start tenant containers only when the orchestrator re-adopts them (`RestartPolicy: no` on workers + boot hook), so a reset can never yield two live WhatsApp sessions.
- `move` is its own operation: the orchestrator streams the whole volume (Docker archive API, `whatsapp-session` included) to a CMEK moves bucket and back into the target; same row and identity; port re-allocated per host; `host_id` flipped once; source volume kept 24 h; object deleted after the target verifies. The user backup export is not a move (it strips the session and streams through the orchestrator).
- Recreate path first: a failure returns the row to `stopped` when a bootable container remains; `error` rows keep their port; recreate does not re-inject pairing-time WhatsApp creds over a live session. Roll with `infra/ops/recreate-tenants.sh` (snapshot + plugin assertion per bot).
- Host scoping: `hosts` table with FK from instances, per-host port allocation, per-user limit counted across hosts, placement = measured usage + committed × explicit overcommit, hard reserve, refuse above 90 %.
- Cloud NAT: dynamic port allocation, 512-8192 ports per VM, ≥2 NAT IPs, alerts on dropped and allocation-failed packets. Default is 64 ports per VM, which drops traffic silently at ~25 bots.
- Alerts keyed on `labels.role`, not one instance id; per-host `expected` vs `running` and reachability as log metrics; `caddy` and `docker-socket-proxy` images mirrored to GAR by digest (Docker Hub limits are per NAT IP).
- Orchestrator VM rebuild: Caddy CA and every key the orchestrator reads come from Secret Manager (23 keys in `.env.runtime` are set only by hand today, including the Telegram manager token); `/home/deploy/backups` synced to a bucket first.

Code, in order, each shippable alone:
1. **DONE 2026-09-14.** `ContainerRuntime` is the interface (`services/container-runtime.ts`, domain types only); `DockerContainerRuntime` + `createDockerClient` live in `services/docker-container-runtime.ts`. `inspect()` is private, its 3 callers use `containerState()`; `listManagedContainers`/`putArchiveBuffer` deleted (no callers). Pure refactor, 450 tests, orchestrator `1dfc406a` in prod.
2. Stable address `https://orchestrator.internal`.
   - **Slice 1 DONE 2026-09-14** (`3f5118c4`): recreate-path fixes, instance-keyed relay rate limits, `TRUST_PROXY` = frontend CIDR.
   - **Slice 2 DONE 2026-09-14** (`f8ff32b1`, migration 0014): relay URLs derived at render time from `ORCHESTRATOR_INTERNAL_URL`; nothing per bot stores them.
   - **Slice 3, pass 0 DONE 2026-09-14** (`b247ca1f`): Caddy serves the internal site (allowlist = the three relay paths, `remote_ip private_ranges` because Caddy answers on the public IP too, verified from the internet) with a named CA `agentforall` (root+key from Secret Manager under `/var/lib/agent-forall/ca`; never `local`, so a stale intermediate in `caddy_data` cannot shadow the seeded root). Same-host bots cannot reach the VM IP on 443 (Docker drops bridge-to-bridge traffic, measured), so Caddy joins `tenant-net` with the alias; the alias retires once no bot runs beside the orchestrator. Both sites proxy to the orchestrator's fixed frontend IP. `TENANT_CA_CERT_PATH` mounts the root into every bot and sidecar with `NODE_EXTRA_CA_CERTS`.
   - **Pass 1 DONE 2026-09-14** (`recreate-tenants.sh --force`, 15/15): every bot carries the CA; the script asserts the relay through the CA per bot.
     **Pass 2 DONE 2026-09-15:** `ORCHESTRATOR_INTERNAL_URL=https://orchestrator.internal` in `.env.runtime` and the first-boot env; a bot switches on its next
     restart or settings change. All 15 bots restarted after the flip the same day (sequential, ~1 min each, 0 failures): every `openclaw.json`
     shows https and a bot's own MCP traffic arrives over it (200/202, plus 405 on the client's GET probe, which is normal). Left before the
     orchestrator leaves `tenant-net`: S-14, and one QR pairing observed over https (sidecar callback).
   - Reserved internal IP and the private zone move to step 6 with the VPC: the zone must be `orchestrator.internal.` (apex A record), never `internal.`, which would shadow GCE internal DNS.
   - Follow-ups: dashboard has no `stopped` state or start action (ops start bots via the API); `whatsapp_creds` dropped 2026-09-14 (boolean `whatsapp_paired`, migrations 0015/0016; see the Supabase egress incident in the handoff); the health monitor reads full rows with `config` every 15 s (~6 GB/month) — a slim projection would cut it to ~2 GB.
3. `hosts` table + identity-token registration (`aud` + instance id ∈ Terraform set, `iat` < 5 min) + per-host circuit breaker + host-scoped queries and ports.
   - **Slice 1 DONE 2026-09-15** (migration 0017): `hosts(id, created_at)`, FK from `instances.host_id` (restrict), the orchestrator registers its own host at boot. Columns arrive with their readers: `address` + `last_registered_at` with registration (slice 2), `status` with placement/move. `InstanceRepository` keeps its host filter until no dev orchestrator shares the prod DB (`local-dev` rows exist there today); per-call host scoping of the Docker-facing reads moves to step 4 with the per-host runtime map.
   - **Slice 2 DONE 2026-09-15** (registration, migration 0018, orchestrator `b995911f`): `POST /internal/hosts/register`, bearer = GCE identity token (`format=full`) minted for `aud` = that URL under `ORCHESTRATOR_INTERNAL_URL`; accepted when Google's signature/expiry/audience pass, `email_verified`, `iat` < 5 min, and the token's `instance_id` is in `WORKER_INSTANCE_IDS` (`hostId=instanceId,...`, a map so a worker can only register as itself); body = private IPv4; writes `hosts.address` + `last_registered_at`. Instance ids, not names: Google signs a token for any VM in any project, and only the id is globally unique. Cert-fetch failure is 502, every rejection 401 with the reason in the log only. Until step 6 this VM is the only worker and is allow-listed to register itself (`startup.sh` reads its own id from metadata); no boot-time caller exists yet, the endpoint was exercised by hand from the VM. The address is dialed by step 4, which must treat host == self as the local runtime.
     For step 6: the worker image bakes the metadata guard without the orchestrator exemption; the worker's curl passes the token via a file, never argv; replacing a VM changes its instance id, so the orchestrator env needs an edit + `compose up -d --force-recreate orchestrator` (the boot path is `--no-recreate`).
   - **Slice 3 DONE 2026-09-15** (per-host circuit breaker, orchestrator `ae9c6a7c`): `HostReachability` pings the host's Docker (5 s abort; dockerode had no timeout) once per pass (concurrent callers share one ping), opens after two consecutive misses, closes on one answer, and logs on transition only (`host unreachable` / `host reachable again`; the first is on the "needs attention" alert). The one-miss tolerance leaves a window of one reconciler run at the start of a real outage in which a stale `provisioning` row can be burned to `error`; accepted. Unreachable: the health monitor writes nothing and reports every bot as `unknown` (the auto-restarter ignores it, budgets intact); the reconciler skips the whole run (resuming provisioning would burn rows to error, expiring pairings needs Docker); memory watch skips the sweep. The reconciler also catches per row, and a failed startup reconcile logs instead of exiting. `/health` = process + DB. Failure mode corrected: the gateway probe is HTTP, so a socket-proxy outage alone never flipped bots; only a wedged dockerd (embedded DNS) did, and dockerd fully down takes the orchestrator with it (no `live-restore`). Boot still waits for Docker; step 6 lifts that and starts each host's gate from a real check.
4. Remote runtime: one `DockerContainerRuntime` per host over mTLS; probes and RPC dial the worker's private IP; pairing sidecar port published on the VPC IP; per-host probe concurrency.
   Design reviewed 2026-09-15; decisions:
   - **4a DONE 2026-09-15** (4a-1 `ed95a061`, 4a-2 `7b2af874`, 4a-3 `b509ac68`; each reviewed, zero log diff on prod). Boot-without-Docker and the optional local host move to the step 6 cutover (nothing exercises them before the orchestrator VM exists).
   - **4a (three behaviour-identical slices):** 4a-1 `HostRuntimes` (`for(hostId)` → `{ runtime, adapters, gate }`, adapters instantiated per host so "wrong host's runtime" is unrepresentable; unknown host = 503 `HOST_UNAVAILABLE`) taken by InstanceManager, PairingManager, OwnerIdentityManager; 4a-2 the loops (health monitor groups by host, gates per host in parallel, one concurrency number per host; reconciler and memory watch likewise); 4a-3 data: repository filter becomes `host_id IN (managed)` where managed = `ORCHESTRATOR_HOST_ID` ∪ `WORKER_INSTANCE_IDS` hosts (membership by config, never by dialability; the dev-DB protection survives), `getActiveGatewayPorts(hostId)`, placement returns the only managed host. Precondition: every host id the widened filter can return must resolve in `HostRuntimes` (a stub whose gate answers false for a worker not yet dialable) before the filter changes, or `hosts.for()` throws inside the loops and pairing expiry. The fleet-wide split of user-facing reads (quota across hosts) waits for the dev DB separation.
   - **4b-1 DONE 2026-09-15** (`8f993606`, reviewed, identical on the local host): `dialUrl` at every HTTP site (five, not three: gateway + WhatsApp probes, auto-restart probe, pairing `waitForLink`, sidecar client), sidecar and bot gateway ports bound to the host's address (loopback locally, the VPC IP on a worker), `restartPolicy` per host (`unless-stopped` local, `no` remote) and the reconciler re-adopt rule (row running + container stopped on a `no` host → start; no gate transition needed, the orchestrator may itself have been down). `publishSidecarPort` folded into `dockerNetwork`.
   - **4b-2 DONE 2026-09-16** (reviewed): dockerode over mTLS (`CONTROL_PLANE_CA_PATH`, `ORCHESTRATOR_CLIENT_CERT_PATH`, `ORCHESTRATOR_CLIENT_KEY_PATH`, files read once at boot, all three or none; boot refuses when a worker is configured without them), remote bundles built from `hosts.address` at boot and on registration (same address = no rebuild), stubs with a closed gate until then. For step 6: the worker compose owns `tenant-net` with `enable_icc: "false"` (the orchestrator never creates networks on workers); the client cert and key are chowned to the container's `orchestrator` uid; never set `DEBUG=modem` on prod (docker-modem prints key material); the worker's server cert needs the VPC IP as SAN (no SNI for an IP host).
   - **4c DONE 2026-09-15** (`cf652d35`, migration 0019 applied, reviewed; boot logs `hosts attached` with `capacityMb 32089`). **STEP 4 COMPLETE.** Follow-up (web): `lib/auth/api.ts` maps every orchestrator 5xx to "server unavailable"; add a `no_placement` mapping before the fleet nears capacity. Note: `hosts.status`/`memory_mb` are read at boot and on registration, so a hand-flipped `draining` needs an orchestrator restart until `move` writes it.
   - **4c (rule fixed 2026-09-15 against prod numbers):** migration 0019: `hosts.memory_mb` (written at registration; self from `os.totalmem()`) and `hosts.status` (`active|draining`). No `instances.memory_mb`: prod runs 15 bots with a 4 GB limit each on a 31 GB host (measured ~7 GB), so a committed-sum rule would refuse every create today. Rule: headroom = 0.9 × (capacity − `HOST_RESERVE_MB`) − measured usage on the host (memory watch's last sweep) − new bot's limit ÷ `PLACEMENT_OVERCOMMIT`; candidates are active, reachable hosts with a known capacity; pick the largest headroom, refuse (503 `NO_PLACEMENT`) when none is ≥ 0. Defaults overcommit 1, reserve 2048 MB accept on prod today (~15.8 GB headroom). Step 6 sets the values per machine type.
   - S-item recorded: `tenant-net` has ICC on, so a bot can already reach another bot's gateway by name with only its bearer; `enable_icc=false` once probes dial by VPC IP.
5. `move` as above, rehearsed on a test bot before any customer bot.
   Design reviewed 2026-09-16; corrections to the bullet: the user backup export cannot carry a move (it execs inside a running container, strips `whatsapp-session`, re-wraps at 1 GB / 50k entries), so `move` streams Docker's archive API (works on a stopped container, raw tar, uid/gid kept) through the orchestrator to the bucket, not a one-off container (S-13 leaves one-offs no credential). Own bucket `agent-forall-moves` with a customer-managed key (KMS ring `agent-forall-moves`, 90-day rotation, lifecycle 2 days, created 2026-09-16). Row flip = one CAS UPDATE (`moveTo`: host, port, `provisioning`, `moved_from_host_id`, `move_object_name`, `moved_at`; migration 0020), then the existing provisioning path restores the volume on the target and promotes; the object is deleted only after the target is healthy. Source container removed at flip time, the named volume kept 24 h and swept by a reconciler pass keyed on the row. Rollback: a failed target (`error`) moves back to the retained volume. Same-host moves refused; `dryRun` (stop, export, verify, delete, start) is the rehearsal path before a second VM exists.
   - **5 DEPLOYED 2026-09-16 13:38 UTC** (`74769e7c`, migration 0020 applied, `MOVES_BUCKET` set). Dry run on the test bot: 111.7 MB tar exported in 11 s, whole call 12 s, bot back and healthy. **STEP 5 COMPLETE**; the first real move is step 6's acceptance test.
   - **5a code DONE 2026-09-16.** Five fresh-eyes rounds on the move code (36 findings) plus a pre-commit whole-diff review of steps 3-5 (three readers, 30 findings), all applied. Rules: `move_object_name` set = the target never ran (rollback allowed; sweeper and `recreate` refuse the row); `move_imported_at` set = the archive is in the target volume (never imported again; a container that started before the import fails the boot). Promotion of a moved row is one write (`completeMove`). A completed move is never rolled back: `recreate` on the target. Rollback refuses a target container that ran. The sweep lives in the manager under the instance lock, re-reads the row, skips provisioning/error/object-carrying rows. A dry run may name the bot's own host (the single-host rehearsal). Migration 0020 (four nullable columns + FK). Runbook: an orchestrator restart before the flip leaves the bot `stopped` (start it; last event `move.started`/`move.exported`); a target host that dies mid-move leaves the row `provisioning` until it answers (hand fix; a `force` rollback is a later item); the admin move call is synchronous for minutes and a client timeout does not cancel it; a rollback after the target ran up to 120 s may need re-pairing; `move_imported_at` is meaningful only while the row is `provisioning` (after a failed boot the target volume is gone and the marker stays); if a rollback is refused because the target container ran and `recreate` is refused too, remove the target container by hand and roll back (the seconds it ran are lost); destroying a failed-move row cannot unlink the WhatsApp device (the session sits in the retained volume), the user removes the linked device by hand.
6. **Worker VM. DEPLOYED 2026-09-16 20:00-21:00 UTC** (orchestrator `ed5938a6`, image `agent-forall-worker-20260916-1700`). Design brief reviewed, then the whole diff by a second reader; three blockers found before any apply (a remote pull that cannot authenticate, a draining target refused by `assertFits`, container-to-host traffic invisible to `DOCKER-USER`). Decisions that differ from the paragraph above, with the reason:
   - Same `default` VPC, hardened: custom subnet mode, network and subnet imported, Private Google Access, a deny-all ingress at 65534 (a tie goes to deny, so the four `default-allow-*` rules are inert; delete them when convenient), tag rules only (`orchestrator` → `worker` 2376 + 18000-18999 + 19000-19999; `worker` → `orchestrator` 443; IAP SSH for both). A new VPC would have meant peering or a NIC move on a `prevent_destroy` VM.
   - The orchestrator never pulls an image on any host: the Docker API pull carries no registry credential (verified on prod: `Unauthenticated request`), so `ensureImagePresent` replaces `ensureImagePulled`, `create` and `move` assert presence, and each host keeps `/etc/agent-forall/pinned-images` warm: pulled at boot, and the nightly housekeeping removes only images that are neither pinned nor behind a container (no nightly re-download). Found live: the prune had deleted the pairing image on the VM, so QR pairing was broken until the hand pull the same day.
   - Worker Docker = dockerd's own TLS listener on the reserved VPC IP (`daemon.json` hosts + a drop-in clearing `-H`, `live-restore`), no Caddy or socket-proxy on a worker. Control-plane PKI issued offline by `infra/ops/issue-control-plane-cert.sh` into Secret Manager (CA key never on a VM; EKUs client-only / server-only; CN = host id, IP SAN). Worker addresses are configuration (`WORKER_ADDRESSES`); the registrar refuses any other address, which closes S-16.
   - Bot isolation on a worker: `tenant-net` with `enable_icc=false` on bridge `af-tenant`, plus INPUT rules (`AF-TENANT` chain) dropping anything a bot sends to the host, since container-to-host traffic never crosses `DOCKER-USER`. Verified from a container: 2376 and a bot port both time out, DNS and internet work.
   - Baked worker image (`infra/images/worker/bake.sh`: Docker, ops agent, the three runtime images; completion via a guest attribute), pinned by name in Terraform; images stay on the boot disk, volumes on the data disk (a fresh data disk lists the baked images, verified). Worker startup is configuration only (`infra/startup/worker.sh`, 61 s to ready); registration is a systemd oneshot minting a fresh identity token per attempt.
   - Files: `infra/main.tf` split by concern (`network.tf`, `dns.tf`, `control-plane.tf`, `orchestrator.tf`, `workers.tf` + `modules/worker`), `startup.sh` split into `startup/common.sh` + role scripts with guard rules and `daemon.json` declared in Terraform per role. Orchestrator VM: `network_ip` = its promoted static 10.164.0.4, `allow_stopping_for_update = false`, tag `orchestrator`, S-14 applied (public site 404s private sources; ops use `agent-forall-admin` on the VM through 172.16.0.10:3000).
   - Code: pairing expiry per reachable host; re-adopt budget in the reconciler (same limits and alert line as the auto-restarter); sidecar host port = gateway port shifted into `SIDECAR_PORT_RANGE_START` (18000); a stopped container is the reconciler's business, the health monitor neither counts nor writes for it (found by the reboot test: its 15 s writes kept the row inside the reconciler's freshness grace forever) and a Docker-restarting container is left alone.
   - Acceptance on the test bot: move to worker-1 (204 in 100 s), healthy there, QR pairing round trip through the fixed sidecar port, recreate (61 s, exec over TLS), dockerd restart with live-restore (container uptime kept), worker reboot re-adopted once, move back; bucket empty, 15/15. worker-1 was `draining` throughout and is `active` since the end of the test.
   - Runbook: add a worker = entry in `workers` (tfvars) → `issue-control-plane-cert.sh server <id> <ip>` → apply → rerun the orchestrator startup + `compose up -d --force-recreate orchestrator` (env lists workers) → insert the host row `draining` first if new bots must wait. Remove a worker = move its bots away, `terraform state rm` its data disk (prevent_destroy), drop the entry, apply, recreate the orchestrator, delete the disk by hand once its snapshot is enough. Rotate a worker cert = new secret version + startup rerun (dockerd restarts under live-restore); leaves expire 2028-09-16, the CA 2036-09-13. SSH is IAP only (`gcloud compute ssh --tunnel-through-iap`); port forwards on Windows need OpenSSH through `start-iap-tunnel`, not plink.
7. Data model split and in-memory fleet state (decided 2026-09-16, one piece, no interim projection): `instances` mixes settings that change monthly (one encrypted JSON blob: provider + key, channels + Telegram token, integrations + relay token, resources) with state that changes every minute (status, health failures, last seen, container id). Split: secrets become individually encrypted fields; the rest become plain, queryable columns (provider name, channel types, memory); runtime state separate from settings. The loops (health monitor, reconciler, memory watch) keep the fleet picture in memory, read the database at boot and on reconcile, and write only transitions, so Postgres is the durable record and no longer a heartbeat bus. Why now: after the 2026-09-14 egress incident the remainder is ~147 MB/day, which is ~4.4 GB/month against the free plan's 5 GB; the full-row read every 15 s crosses the line at roughly 17 bots. Verify with the Supabase egress chart before/after.

Verify each step on a second VM in the same project before touching the current one. Existing VM becomes `worker-1`; orchestrator moves to its own VM last.

## Later

- One deploy script (build, pin the digest in Terraform and the VM env, apply, roll, verify): today the digest is carried by hand in three places on every roll.

- ~100 bots or credits gone: 1-year CUD (~$5/bot) or Hetzner AX workers (~€1/bot), control plane unchanged.
- GKE only with an engineer who has run it, or >500 bots.

## Not doing

GKE, Agent Sandbox (Failed pods not recreated), Autopilot (~$19/bot), Fly (unreplicated volumes), Cloud Run (no persistent disk, 60-min request cap), per-bot public DNS or Caddy on workers.
